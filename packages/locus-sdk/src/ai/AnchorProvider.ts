/**
 * Compatibility re-export for AnchorProvider.
 * Canonical implementation lives in LocusProvider.ts.
 */
export * from './LocusProvider';
export { LocusProvider as AnchorProvider } from './LocusProvider';
